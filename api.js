// const data = require("./data.json");
// const list = [];

// for (const key in data) {
//     if (Object.hasOwnProperty.call(data, key)) {
//         const element = data[key];
//         list.push(element);
//     }
// }

// const reversed = list.reverse();

const date = new Date();
// date.setFullYear(2017, 2, 13);

console.log(date.getTime());
